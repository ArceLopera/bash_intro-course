title: Bash_Intro
description: Basic course in Bash
template:
  repo: bash_intro-template
  name: bash_intro
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
